const CheckoutForm = ({ setOrder }) => {

    const prepareOrder = () => {
        setOrder({

        })
    }

    return (
        <form>
            <input />
            <button onClick={prepareOrder}>Pagar</button>
        </form>
    )
}

export default CheckoutForm